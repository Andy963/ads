## 说明
本项目已精简为仅使用 Codex：不再支持 ADS 工作流与相关命令（例如 `/ads.*`、`/agent`）。

## 对话与执行
- 先澄清必要信息，再开始实现。
- 修改代码后运行项目校验（lint / typecheck / tests），除非用户明确要求跳过。

## 安全
- 不要泄露任何密钥、Token、个人隐私或仓库敏感信息。
- 避免破坏性命令；如确有必要，先向用户说明风险并获得确认。

## Workspace（可选）
- 若当前目录缺少 `.ads/workspace.json`，系统会回退使用内置模板/规则；这不影响基本对话与开发。
